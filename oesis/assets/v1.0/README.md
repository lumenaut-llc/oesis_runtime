# Runtime Assets v1.0

This directory is the explicit opt-in future lane for runtime asset overrides.

Files here are layered on top of the frozen root `oesis/assets/` defaults only
when the `oesis-v10-*` commands or equivalent environment selection are used.
