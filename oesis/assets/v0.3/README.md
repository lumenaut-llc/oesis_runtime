# v0.3 lane assets

Additive overrides for the `v0.3` runtime lane (flood-capable parcel kit).

v0.3 extends v0.2 with:

- **flood-node** as an optional parcel-specific hazard module
- Dedicated `flood.low_point.snapshot` observation family
- Flood-aware parcel explanation
- Three-node parcel context (bench-air + mast-lite + flood-node)
