# v1.0 Config Overrides

Place additive or replacement inference-config files for the explicit `v1.0`
lane here.

If a file is not overridden here, the helper scripts keep using the frozen root
`v0.1` config file as the fallback for that opt-in lane.
