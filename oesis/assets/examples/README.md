# Legacy path (deprecated)

This unversioned path is retained only as a migration marker.

Canonical baseline fixtures now live under `../v0.1/examples/`.
Future-lane overrides live under `../v1.0/examples/` (and later versioned lanes).
