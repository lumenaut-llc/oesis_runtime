# v1.5 Example Overrides

Place additive or replacement runtime example fixtures for the explicit `v1.5`
bridge lane here.

If a file is not overridden here, helper scripts keep using the frozen baseline
`v0.1` example as fallback for that lane.

Current bridge-stage fixtures:

- `house-state.example.json`
- `house-capability.example.json`
- `equipment-state-observation.example.json`
- `source-provenance-record.example.json`
- `intervention-event.example.json`
- `verification-outcome.example.json`
