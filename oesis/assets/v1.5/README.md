# Runtime Assets v1.5

This directory is the explicit bridge-stage lane for runtime asset overrides.

Files here are layered on top of the frozen baseline lane
`oesis/assets/v0.1/` only when lane-aware commands or equivalent environment
selection are used.

This lane carries bridge support-object examples used for measurement-to-
intervention workflows.
