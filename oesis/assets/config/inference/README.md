# Inference Config (v0.1 default)

These files are the frozen default inference-config surface for the `v0.1`
runtime lane.

Future-lane overrides belong under `../../v1.0/config/inference/` and should be
selected only through the explicit `oesis-v10-*` commands or equivalent
environment overrides.
